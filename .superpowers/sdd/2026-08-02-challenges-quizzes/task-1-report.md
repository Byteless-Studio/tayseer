# Task 1 Report: Shared quiz types + Vocabulary quiz

## Status: DONE

## Commit
`563d117` — "Add vocabulary roots & patterns quiz" (on branch `worktree-challenges-quizzes`, worktree at `C:\Users\Usaid\Projects\tayseer\.claude\worktrees\challenges-quizzes`)

## Files created
- `src/routes/challenges/-quiz-types.ts` — exports `StrictQuizItem` exactly as specified in the brief (`q`, `a`, `options: [string, string, string, string]`, `correctIndex: 0 | 1 | 2 | 3`). Structurally assignable to the existing `QuizItem` from `-arabic-101.types.ts`, so no changes to `QuizCarousel` were needed.
- `src/routes/challenges/vocabulary/index.tsx` — the vocabulary quiz route at `/challenges/vocabulary/`. Header uses the same visual shell as `src/routes/cognates/index.tsx` (bg-beige band, `mx-auto w-[min(1080px,calc(100%-2rem))]` container, `font-serif` h1, eyebrow label), plus a back link to `/challenges` and an intro blurb (with RTL `dir="rtl" lang="ar"` spans styled `'Noto Naskh Arabic', serif`) explaining root + pattern/wazn. Body renders `<QuizCarousel items={quizItems} />`.

## Files modified (regenerated, included in commit)
- `src/routeTree.gen.ts` — auto-regenerated by the TanStack Router vite plugin during `npm run build`, includes the new `/challenges/vocabulary/` route.

## Quiz content
`quizItems` has **41 entries** (verified via `grep -c "correctIndex:"`):
- 18 questions covering the 9 verb patterns listed in the brief (فَعَلَ، فَاعَلَ، أَفْعَلَ، فَعَّلَ، تَفَعَّلَ، تَفَاعَلَ، اِنْفَعَلَ، اِفْتَعَلَ، اِسْتَفْعَلَ), 2 each. Note: the brief's heading says "The 10 verb patterns" but only lists and exemplifies 9 patterns with roots (and states "(18 min)" = 9×2), so I implemented the 9 patterns actually enumerated rather than inventing a 10th unlisted pattern.
- 13 questions covering the 6 noun-pattern categories (فَاعِل، مَفْعُول، فَعَّال، مِفْعَال/مِفْعَل/مِفْعَلة، مَفْعَل/مَفْعِل، مصدر فِعَالة/فُعُول/تَفْعِيل), 2 each except the مصدر category which got 3 (one per named sub-pattern) to cover all three explicitly named patterns.
- 10 mixed questions, one per required root (ك-ت-ب، د-ر-س، ع-ل-م، خ-ر-ج، د-خ-ل، ف-ت-ح، ك-س-ر، ج-م-ع، ن-ص-ر، ق-ر-ب), each asking for root/pattern/meaning identification of a derived word.

Every `a` field names the pattern and explains what it grammatically/semantically conveys (causative, reciprocal, reflexive, passive, instrument, place, professional-intensive, verbal noun, etc.), not just restating the correct option. Correct-answer position (`correctIndex`) is distributed cyclically across 0–3 rather than clustered at one index, for a better quiz UX.

## Verification
- `npm run build`: pass, no TypeScript errors. `routeTree.gen.ts` regenerated automatically with the `/challenges/vocabulary/` route wired in.
- Manual check: ran `npm run dev` (came up on port 3001 since 3000 was in use), fetched `http://localhost:3001/challenges/vocabulary/` via curl, and confirmed in the rendered SSR HTML: the page title, the beige header band, the back-to-Quizzes link, the intro blurb with RTL Arabic spans, the first quiz question (روot ك-ت-ب / وزن فَعَلَ) rendering correctly, and the four `a) b) c) d)` option buttons from `QuizCarousel`. Did not click through every question interactively (no browser automation tool used), but the carousel component itself is unmodified and pre-existing/known-working, and the data shape passed type-checking under `StrictQuizItem`, which is the primary risk this task's verification step calls out.

## Concerns
- None blocking. One judgment call: brief text says "10 verb patterns" but enumerates only 9 with roots/examples and a consistent "(18 min)" = 9×2 question count — I followed the enumerated list and count rather than the summary number, since the explicit list is the authoritative content requirement.
- Did not modify `-arabic-101.types.ts` or `QuizCarousel.tsx`, per instructions.
- Did not touch `src/routes/challenges/index.tsx` (still the "Coming Soon" placeholder) — out of scope for this task, presumably wired up by a later task in the plan.
- Left the pre-existing unrelated working-tree changes (`src/routes/api/publish/$.ts`, `src/routes/arabic-with-mufti-saim/-arabic-101.types.ts`, `public/tayseer-logo-large.png`) untouched and unstaged — they predate this task and are not part of this task's scope.
