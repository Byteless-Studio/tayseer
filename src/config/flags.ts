// Feature flags — flip these to enable/disable features without touching UI code.

export const flags = {
  /** Show the dark/light/auto theme toggle in the header. */
  themeToggle: false,
} as const
